
<div id="comment_new">

	<%= form_for(@comment) do |f| %>
	  <% if @comment.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

	      <ul>
	      <% @comment.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

    <div class="field">
      <% if user_signed_in? and current_user.picture.present? -%>
        <%= image_tag current_user.picture, :class => 'borda_vinho' %>
      <% end -%>
      <%= f.text_area :body %>
    </div>

    <%= f.hidden_field :proposal_id %>

    <div class="actions">
      <%= image_submit_tag '/images/comment.png', :class => 'submit_comment' %>
    </div>
  <% end %>

</div>
