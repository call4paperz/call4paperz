<div id="top">
	<div id="pic">
		<div id="intro">
			<p>
				<strong>Organize your call for papers.</strong>
				...and let audience decide who speaks in the conference!
			</p>
		</div>
		<div id="howto"></div>
		<div id="twitter"></div>
	</div>
</div>

<div id="content">
	<div id="most_recent_events">
		<div class="head">
			<%= image_tag '/images/most_recent_events.png' %><br />
			<p><strong>Decide who goes to the conference!</strong></p>
		</div>

		<% @events.each do |event| %>
			<div class="event">
				<p><strong><%= event.name %></strong></p>

				<p><%= truncate (event.description, :length => 170) %></p><br/>

				<div class="buttons">
					<%= image_tag '/images/more.png', :class => 'more' %>
					<%= link_to pluralize (event.proposals.size, 'proposal'), root_path %>
					<%= link_to pluralize (event.comments.size, 'comment'), root_path %>
          <%= render 'shared/tweet_button', :text => "This event is on a call for papers! Check it out. #call4paperz", :url => event_url(event) %>
          <%= render 'shared/facebook_like', :url => event_url(event) %>
				</div>

			</div>
		<% end %>
		<p class="prop_link">We have <span>12</span> opened call for papers available to vote. Click to see all.</p>
		<br/>

	</div>

	<div id="most_recent_comments">

		<div class="head">
			<%= image_tag '/images/most_recent_comments.png' %><br />
			<p><strong>What people think about last proposals.</strong></p>
		</div>

		<div id="comments">
			<% @comments.each do |comment| %>
				<div class="comment">
					<div class="comment_pic">
						<%= image_tag comment.user.picture, :class => 'borda_vinho' if comment.user.picture.present? %>
					</div>
					<div class="comment_detail">
						<p><%= truncate(comment.body, :length => 60) %></p>
						<p><small><%= comment.user.name %></small></p>
					</div>
					<div style="clear:both"></div>
					<%= image_tag '/images/linha.png' %>
				</div>
			<% end %>
			<%= link_to "all comments", root_path, :class => 'all' %>
		</div>

	</div>
</div>
